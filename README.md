git clone https://github.com/nnnhurt/demo .

notepad (Get-PSReadLineOption).HistorySavePath

Откроется блокнот, в него жмем Cntrl A, все стираем и Cntlr S. Закрываем терминал через иконку с мусоркой и создаем новый.

Remove-Item -Recurse -Force .git (команда чтобы удалить .git)

Проверить ls -h (должно быть пусто)

—-----------

uv sync (чтобы и венв создать и чтобы зависимости)

git init

git config user.name “user”
git config user.email “nfnfdkfkdj@mail.ru”

активация венва
.\.venv\Scripts\activate.ps1

Идем в DBeaver, открываем подключение к постгресу, выбираем галочку “Показать все базы данных”
Пароль: 123456
У нас там раздел Databases, жмем правой кнопкой мыши и создать БД (Create new Databse). Имя demo. 

Появилась база Demo. 

Идем в наш проект.
(миграции)
python3 manage.py makemigrations ( для вида)
python3 manage.py migrate

(загрузка данных)
python3 manage.py scriptdb 

(запуск сервера - оставить включенным перед сдачей)
python3 manage.py runserver

—--------------------------------------------

если вдруг понадобится очистить базу 
python3 manage.py flush
потом написать yes и удалить папку media

для загрузки - еще раз python3 manage.py scriptdb
---------------------------------------------
надо прочитать диапазоны скидок из задания 
(должно быть что то вроде 0-12%, 12-17%, 17-19%, в задании будет точно указано)
их надо поменять в файле demo/settings.py в переменной SKIDKA_RANGES
числа с точкой там работают

аналогично с настройками названия сайта и скидки для раскрашивания карточки
(но они вроде не должны меняться)
---------------------------------------------
если вдруг в руководстве по стилю будут не пурпурные цвета - их можно
отредактировать в файле static/css/style.css, в самом начале.
просто вписать указанные коды цветов вместо имеющихся

НЕ ЗАБЫТЬ
сделать коммиты (лучше просто каждую папку отдельно закоммитить)
сделать ворд документ со скринами
сделать диаграмму процесса разработки в пдф (через приложение или сайт draw.io)
удалить этот файл